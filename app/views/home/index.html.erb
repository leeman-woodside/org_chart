<!DOCTYPE html>
<html>
<head>
  <title>Org Chart Challenge</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px; 
    }

    h1 {
      margin-bottom: 20px; 
    }

    form {
      margin-bottom: 20px;
    }

    input[type="file"] {
      margin-right: 10px;
    }

    button {
      padding: 10px 20px; 
      background-color: #007bff; 
      color: white;
      border: none;
      cursor: pointer;
    }

    button:hover {
      background-color: #0056b3; 
    }
  </style>
</head>
<body>
  <h1>Org Chart Challenge</h1>

  <%= form_tag(import_csv_path, multipart: true) do %>
    <%= file_field_tag :csv_file %>
    <%= submit_tag 'Upload CSV' %>
  <% end %>

  <button id="exportButton">Export Database as CSV</button>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      document.getElementById('exportButton').addEventListener('click', function() {
        window.location.href = '/export_csv';
      });
    });
  </script>
</body>
</html>